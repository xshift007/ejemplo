spring:
  application:
    name: book-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/bdmicros
    username: root
    password: Chile&2022
  jpa:
    hibernate:
      ddl-auto: update